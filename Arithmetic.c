#include <stdio.h>

int main() {

    /*
    arithmetic operators
        + (addition)
        - (subtraction)
        * (multiplication)
        / (division)
        % (modulus)
        ++ (increment)
        -- (decrement)
    */

   int x = 5;
   int y = 2;

   float z = (float)x/y;

   printf("%f", (float)z);

    return 0;
}